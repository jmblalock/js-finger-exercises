var age = 33;

if (age >= 18) {
	goVote();
}

function goVote() {
	console.log('every vote counts');
}

var isEnrolled = true;

if (isEnrolled) {
	console.log("take the class");
} else {
	console.log("enroll first!");
}